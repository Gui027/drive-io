import styled from 'styled-components';

export const Box = styled.header`
  height: 42px;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;

  > img {
    width: 170px;
    height: 50px;
  }
  `
