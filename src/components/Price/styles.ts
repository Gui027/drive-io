import styled from 'styled-components';

export const Box = styled.header`
font-size: 22px;
  margin-top: 80px;
  margin-bottom: 80px;
  color: #1A2254;
  text-align: center;
  
  span {
    color: #00A5FA;
  }

  h3 {
    margin: 20px 0px;
   color: rgba(30, 37, 94, 0.7);
   font-weight: 400;
  }
`